<?php

function CalcPrice($priceInput){
			var $tempPriceResult = 2 * ($priceInput - $price) / (($priceInput + $price) / 2);
			$this->priceScore = $priceInput > $price ? 1 : 1 * (1 + $tempMathResult1);
		}

	class calculator {
		function CalcScore($gadget1, $gadget2){
				
		}
	}

?>